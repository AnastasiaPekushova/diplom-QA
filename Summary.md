*Отчет по итогам автоматизации тестирования*

1. Что было запланировано(з) и что реализовано(р):
   функциональные тесты: з+, р+
   тесты sql: з+, р+
   тесты валидации: з+, р+
   Настройка Docker Compose: з+, р+
   Allure-отчеты: з+, р+

2. Сработавшие риски:
   В ходе настройки Docker Copmose возникли сложности с эмулятором банка. Были прописаны верные пути для карт, БД и прописан дополнительно порт.
   Трудности с селекторами: при отправке пустого поля CVC/CVV поле владелец оставалось заполненным фактически, но пустым для приложения, что ломало тесты

3. Общий итог по времени:
   На составление тестовой документации ушло минимальное количество времени, меньше чем планировалось.
   Гораздо больше запланированного было потрачено на составление Docker Compose - сначала неподгружалась БД, после были нюансы с эмулятором.
   При написании автотестов для поля CVC/CVV отправка пустого поля ломало тесты и поиски селектора, чтобы обойти ошибку заняли также сильно больше времени.
   И в целом запуск автотестов с условием работы Docker Compose, когда они отрабатывали корректно занимало порой 2 часа каждый прогон всвязи со слабым железом моего оборудования, что будет в перспективе исправлено